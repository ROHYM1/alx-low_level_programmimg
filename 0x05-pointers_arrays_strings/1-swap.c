#include <stdio.h>
#include "main.h"

 /**
  * swap_int - swap the value of two integers
  * @a: first number
  * @b: second numbe
  * Return: nothing
  */

void _swap(int *a, int *b);

{
	int k = *b;

	*b = *a;

	*a = k;
}
