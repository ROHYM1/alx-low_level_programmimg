#include <stdio.h>
#include "main.h"

 /**
  * _swap - swap the value of two integers
  * @a: first number
  * @b: second numbe
  * Return: returns result
  */

void _swap(int *a, int *b);

{
	int c = *a;

	*a = *b;

	  *b = c;

}
