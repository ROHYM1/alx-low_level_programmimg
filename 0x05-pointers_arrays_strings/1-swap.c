#include <stdio.h>
#include "main.h"

 /**
  * swap_int - swap the value of two integers
  * @a: first number
  * @b: second numbe
  * Return: returns result
  */

void swap_int(int *a, int *b);

{
	int c = *b;

	*b = *a;

	  *a = c;

}
